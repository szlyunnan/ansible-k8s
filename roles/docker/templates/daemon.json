{
    
    {% if DOCKER_WORK|default(false) %}

    "registry-mirrors": ["{{ CN_REGISTRY }}"],
    "storage-driver": "overlay2",
    "graph": "{{ DOCKER_WORK }}"

    {% else %}

    "registry-mirrors": ["{{ CN_REGISTRY }}"],
    "storage-driver": "overlay2"
    
    {% endif %}

}
